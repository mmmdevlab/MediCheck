info:
  name: Revoke Caregiver Access
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:3000/api/caregivers/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
