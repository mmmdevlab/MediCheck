info:
  name: Grant Access - Invalid Email
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/caregivers
  auth: inherit
  body:
    mode: json
    json: |
      {
        "caregiver_email": "not-an-email"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
