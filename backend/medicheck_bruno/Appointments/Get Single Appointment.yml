info:
  name: Get Single Appointment
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/appointments/1"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://http://localhost:3000/api:3000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
