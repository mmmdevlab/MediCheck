info:
  name: Get Appointment - Unauthorized (403)
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/appointments/999"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000/api
  scripts:
    - type: tests
      code: |-
        test("Returns 403 or 404", function() {
            const status = res.getStatus();
            expect([403, 404]).to.include(status);
          });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
