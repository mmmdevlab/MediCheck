info:
  name: "Caregiver: patient's history"
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/logs/patients/:patientId?days=7"
  headers:
    - name: Authorization
      value: Bearer {{caregiver_token}}
  params:
    - name: days
      value: "7"
      type: query
    - name: patientId
      value: "{{patient_token}}"
      type: path
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
