info:
  name: List My Caregivers
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/caregivers"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
