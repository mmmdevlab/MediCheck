info:
  name: Revoke Caregiver Access
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/caregivers/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
