info:
  name: View Patient Appointments
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/patients/5/appointments"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
