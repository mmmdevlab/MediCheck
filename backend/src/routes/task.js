// const express = require("express");
// const router = express.Router();
// // const taskController = require("../controllers/taskController");
// const verifyToken = require("../middleware/verifyToken");

// router.use(verifyToken);

// router.post("/", taskController.createSupportRequest);
// router.get("/", taskController.getMySupportRequests);
// router.get("/:id", taskController.getSupportRequestById);
// router.put("/:id", taskController.updateSupportRequest);
// router.delete("/:id", taskController.deleteSupportRequest);

// router.get("/assigned/requests", taskController.getAssignedSupportRequests);
// router.patch("/:id/respond", taskController.respondToSupportRequest);
// router.patch("/:id/complete", taskController.completeSupportRequest);

// module.exports = router;
